class Game:
    def __init__(self, state, player='P'):
        if state is None:
            self.gameState = dict()

            for i in range(0,5)
                for j in range(0,5)
                    self.gameState[i, j] = '.'

    def move(self):
        print("Hello")
    
    def capture(self):
        print("World")

g = Game()
print(g.state)